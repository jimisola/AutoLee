# Name,     Type, SubType,  Offset,   Size,     Flags
# No SPIFFS/LittleFS partition: the source never mounts a filesystem (the
# INDEX_HTML web UI is a compiled-in string, not a served file), so the
# space that "Minimal SPIFFS" reserved on Arduino goes to coredump instead.
nvs,         data, nvs,      0x9000,   0x6000,
otadata,     data, ota,      0xf000,   0x2000,
ota_0,       app,  ota_0,    ,         0x1E0000,
ota_1,       app,  ota_1,    ,         0x1E0000,
coredump,    data, coredump, ,         0x10000,
